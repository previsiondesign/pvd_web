# Raw project assets — multi-GB renders/video/PDFs, exceed GitHub's 100 MB file limit.
# Web-ready copies the mockups actually use live in website-mockups/shared/images/.
images/projects/

# Reference docs kept local — references PDF contains third-party contact info
docs/

# Palette reference images (third-party samples; hex values live in the variant CSS)
pallettes/

# Hero source masters (137 MB of PNGs) — web-ready WebP derivatives are committed
# under website-mockups/shared/images/hero/ and rebuilt by scripts/build-hero-images.py
projects/

# Local scratch / machine-specific
_logotest.html
.claude/settings.local.json
Thumbs.db
.DS_Store
